const ItemList = () => {
    return (
        <ul>
            {/* {productsComp} */}
            {products.map(prod => <li key={prod.id}>{prod.name}</li>)}
        </ul>
    )
}

export default ItemList